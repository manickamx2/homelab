- name: Bootstrap cluster-wide resources
  hosts: localhost
  vars:
    kubeconfig_path: "{{ playbook_dir }}/../../kubeconfig.yaml"
    gateway_api_version: "v1.4.1"
  vars_files:
    - vault.yml
  roles:
    - cluster_bootstrap